import { CoverSrcsetPipe } from './cover-srcset.pipe';

describe('CoverSrcsetPipe', () => {
  it('create an instance', () => {
    const pipe = new CoverSrcsetPipe();
    expect(pipe).toBeTruthy();
  });
});
