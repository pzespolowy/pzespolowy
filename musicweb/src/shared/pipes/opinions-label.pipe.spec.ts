import { OpinionsLabelPipe } from './opinions-label.pipe';

describe('OpinionsLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new OpinionsLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
